{
  "name": "hardhat-project",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:alchemy": "hardhat run --network sepolia-alchemy scripts/deploy.js",
    "deploy:infura": "hardhat run --network sepolia-infura scripts/deploy.js",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.3"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.9.2"
  }
}
